# traefik.yml
api:
  dashboard: true
  insecure: false

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
  # Client panel access port (like cPanel's :2083).
  # Any client domain pointing to this server can reach the panel via :2083.
  clientpanel:
    address: ":2083"

providers:
  docker:
    endpoint: "tcp://docker-socket-proxy:2375"
    exposedByDefault: false
  file:
    filename: /etc/traefik/dynamic_conf.yml

certificatesResolvers:
  myresolver:
    acme:
      storage: /acme.json
      httpChallenge:
        entryPoint: web
